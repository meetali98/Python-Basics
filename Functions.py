# FUNCTIONS ---> allows us to create block of code than can be easily many times,
#without needing to constantly rewrite the entire block of code

def say_hello(name='default'): # uses default when no parameter is passed
    print(f'hello {name}')
say_hello('Meetali')

